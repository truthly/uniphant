name: uniphant

on: push

jobs:
  uniphant:
    name: uniphant
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: install
        run: ./install.sh
        shell: bash